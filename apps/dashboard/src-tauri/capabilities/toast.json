{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "toast",
  "description": "Capabilities for the screen-level notification toast window",
  "windows": ["toast"],
  "permissions": ["core:default"]
}
